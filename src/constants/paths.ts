export const PATHS = {
  ROOT: '/',
  ONE: '/one',
  TWO: '/two',
  THREE: '/three',
} as const;

export const API_PATHS = {
  TEST: '/test',
  TEST_ID: '/test/:testId',
  TEST_POST: '/test',
} as const;
